[English](README.md) | [Français](README.fr.md) | **العربية**

<div align="center" dir="rtl">

# @geoalgeria/normalize

**مفاتيح البحث لأسماء الأماكن الجزائرية، بالعربية وبالفرنسية. طيٌّ واحد في كل مكان.**

[![npm](https://img.shields.io/npm/v/@geoalgeria/normalize)](https://www.npmjs.com/package/@geoalgeria/normalize)
[![npm downloads](https://img.shields.io/npm/dm/@geoalgeria/normalize)](https://www.npmjs.com/package/@geoalgeria/normalize)
[![Code: MIT](https://img.shields.io/badge/Code-MIT-green.svg)](LICENSE)

</div>

<div dir="rtl">

كتابة `bejaia` يجب أن تجد **Béjaïa**، وكتابة `بجاية` يجب أن تجد **بِجَايَة**. لا يتحقق ذلك إلا إذا
ردّ كل برنامج في السلسلة الاسمَ إلى النص نفسه حرفًا بحرف: الموقع الذي يبحث في المتصفح، وبناء
الإصدار الذي يملأ فهرس النص الكامل، والهاتف الذي يبحث دون اتصال. هذه الحزمة هي ذلك الطيّ، ولا
شيء غيره. جزء من [GeoAlgeria](https://github.com/yasserstudio/geoalgeria).

</div>

```bash
npm install @geoalgeria/normalize
```

```js
import { conservativeKey, looseKey, tokenize, searchKeys, explain, rules, NORMALIZE_VERSION } from "@geoalgeria/normalize";

conservativeKey("Béjaïa");         // "bejaia"
conservativeKey("Sidi-Bel-Abbès"); // "sidi bel abbes"
conservativeKey("El M’Ghair");     // "el m ghair"
conservativeKey("بِجَايَة");           // "بجاية"
conservativeKey("الجـــزائر");        // "الجزاير"

looseKey("قسنطينة");                // "قسنطينه"
looseKey("مصطفى");                  // "مصطفي"
tokenize("Sidi-Bel-Abbès");        // ["sidi", "bel", "abbes"]

searchKeys("قسنطينة");
// { conservative: "قسنطينة", loose: "قسنطينه", tokens: ["قسنطينة"], looseDiffers: true }

explain("قسنطينة").applied;        // ["ar.taa-marbuta-haa"]
rules.length;                      // 24

NORMALIZE_VERSION;                 // 1
```

<div dir="rtl">

## المفتاح المحافظ

يحلّ **المفتاح المحافظ** الطرقَ المختلفة لكتابة الاسم الواحد، ولا يطوي شيئًا يغيّر الحرف الذي
يراه القارئ:

| القاعدة | مثال |
| --- | --- |
| أشكال العرض العربية تعود إلى حروفها الأصلية | `ﺃﺩﺭﺍﺭ` تصير `ادرار` |
| صور الألف (بهمزة فوقها أو تحتها، وبالمدّة، وألف الوصل) تصير ألفًا مجردة | `أدرار` و`إدرار` و`آدرار` و`ٱدرار` كلها `ادرار` |
| الهمزة على الواو أو الياء تُردّ إلى الحرف المجرد | `الجزائر` تصير `الجزاير` |
| التطويل يُحذف | `الجـــزائر` تصير `الجزاير` |
| الحركات وسائر العلامات العربية المركّبة تُحذف | `بِجَايَة` تصير `بجاية` |
| اللواحق اللاتينية تعود إلى حرفها الأصلي، مركّبةً كانت أو مفكّكة | `Béjaïa` تصير `bejaia` |
| اللاتينية الموسّعة أ وحروف اللاتينية الموسّعة ب المشكّلة تُطوى كذلك | `Bāb el Oued` تصير `bab el oued`، و`Ǧerǧer` تصير `gerger` |
| حرف البربرية اللاتينية يبقى حرفه ولا يفقد إلا كِبَره | `TAMAZIƔT` تصير `tamaziɣt` |
| الأرقام العربية الهندية والعربية الهندية الشرقية تصير أرقامًا لاتينية | `٣٤٥` و`۳۴۵` كلاهما `345` |
| صور الفاصلة العليا والشرطة تفصل بين الكلمات | `El M'Ghair` و`El M’Ghair` تصيران `el m ghair` |
| علامات الترقيم تفصل بين الكلمات ولا تصل إلى المفتاح أبدًا | `Alger, Oran (Es Senia).` تصير `alger oran es senia`، و`وهران، تلمسان؟` تصير `وهران تلمسان` |
| المسافات تُدمج، وحدود الكلمات تبقى داخل المفتاح | `  Oran   El Bahia ` تصير `oran el bahia` |
| حالة الأحرف تُردّ إلى الصغيرة | `SÉTIF` تصير `setif` |
| الحرف الذي لا يسمّيه أي جدول يبقى كما هو ولا يُحذف | `Tamaziɣt` تصير `tamaziɣt` |

## المفتاح الأوسع

**المفتاح الأوسع** هو المفتاح المحافظ مع مكافأتين اثنتين لا غير:

| القاعدة | مثال |
| --- | --- |
| الألف المقصورة والياء حرف واحد | `مصطفى` تصير `مصطفي` |
| التاء المربوطة والهاء حرف واحد | `قسنطينة` تصير `قسنطينه` |

فُصلتا عن المفتاح المحافظ حتى تُرتَّب المطابقة الناتجة عنهما دون المطابقة التامة بدل أن تكون
مثلها تمامًا. و`searchKeys` تخبر هل طُبّقت إحداهما فعلًا:

</div>

```js
searchKeys("تيزي وزو").looseDiffers; // false، المفتاحان نصّ واحد
searchKeys("قسنطينة").looseDiffers;   // true
```

<div dir="rtl">

## الكلمات ومجموعة الفواصل

المفتاحان كلاهما قائمةُ الكلمات موصولةً بمسافة واحدة، فتبقى الكلمة الأخيرة الناقصة قابلة
للإكمال، ولا يُطابَق الاستعلام أبدًا على سلسلة حروف ملتصقة طويلة. و`tokenize` هي ذلك التقطيع،
وعلى الفهرس الذي يبنيه المستهلك أن يوافقه حرفًا بحرف. تنتهي الكلمة عند هذه الرموز وحدها:

- المسافات: `U+0009` إلى `U+000D`، و`U+0020`، و`U+00A0`، و`U+1680`، و`U+2000` إلى `U+200A`،
  و`U+2028`، و`U+2029`، و`U+202F`، و`U+205F`، و`U+3000`؛
- صور الفاصلة العليا: `'` و`` ` `` و`ʼ` و`‘` و`’` (`U+0027`، `U+0060`، `U+02BC`، `U+2018`، `U+2019`)؛
- صور الشرطة: `-` و`U+2010` إلى `U+2015`؛
- علامات الترقيم: `U+0021` إلى `U+0026`، و`U+0028` إلى `U+002C`، و`U+002E`، و`U+002F`،
  و`U+003A` إلى `U+0040`، و`U+005B` إلى `U+005F`، و`U+007B` إلى `U+007E`، وعلامتا التنصيص
  `U+00AB` و`U+00BB`، والفاصلة العربية `U+060C`، والفاصلة المنقوطة `U+061B`، وعلامة الاستفهام
  `U+061F`، والنقطة `U+06D4`، و`U+2016` إلى `U+2017`، و`U+201A` إلى `U+2027`، و`U+2030` إلى
  `U+205E`.

وكل ما عداها إما مطويّ أو محذوف أو جزء من كلمته. أما الرموز غير المرئية، ومنها شرطة السطر
الاختيارية وعلامات اتجاه النص، فتُحذف ولا تُعامل حدودًا، لأنها ليست ما يراه القارئ. فالمفتاح
إذن لا يحمل علامات ترقيم أبدًا، وهذا نفسه ما يبقي هذا التقطيع مطابقًا لتقطيع محرّك البحث في
النص الكامل.

## جدول القواعد المراجَع

`rules` هو الجدول نفسه، مجمّدًا ومنشورًا. فالمساواتُ التي تقرّرها هذه الحزمة بين صور كتابة
الأسماء الجزائرية ينبغي أن يقرأها من يقرأ اللغة لا الشيفرة: لذلك تحمل كل قاعدة معرّفها، وصنفها،
والكتابة التي تخصّها، وسلاسل النقاط البرمجية التي تحوّلها وإلى ماذا، وجملةً واحدة يستطيع ابن
اللغة أن يجادلها، وسجلَّ من راجع تلك الجملة ومتى.

```js
import { rules } from "@geoalgeria/normalize";

rules.find((rule) => rule.id === "ar.taa-marbuta-haa");
// {
//   id: "ar.taa-marbuta-haa", class: "loose", script: "arab",
//   from: ["ة"], to: ["ه"],
//   why: "A name ending in taa marbuta is commonly typed with haa, and the reverse, ...",
//   reviewed: { reviewedBy: "yasserstudio", reviewedAt: "2026-09-06" },
// }
```

يُقرأ الحقلان `from` و`to` من الجداول التي يشغّلها مسار المفتاح نفسه، فلا يفترق ما تقوله القاعدة
عمّا تفعله. والهدف الفارغ يعني أن الحرف يُحذف، والمسافة الواحدة تعني أنه ينهي كلمة. والقاعدتان
المرفوضتان تحملان الطيّ الذي ترفضانه، ليكون المرفوض ظاهرًا ظهورَ المقبول.

| القاعدة | الصنف | الكتابة | السبب |
| --- | --- | --- | --- |
| `ar.presentation-forms-b` | `canonical` | `arab` | أشكال العرض في كتلة Arabic Presentation Forms-B ترسم الحروف نفسها التي في الكتلة العربية، فالاسم المنسوخ من ملف PDF يعود إلى الحروف التي يُكتب بها. |
| `ar.presentation-forms-a` | `canonical` | `arab` | شكلا ألف الوصل والألف المقصورة في كتلة Arabic Presentation Forms-A هما الحرفان نفسهما في الكتلة العربية. |
| `ar.lam-alef-ligature` | `canonical` | `arab` | لام ألف صورة واحدة لحرفين، فتُردّ إلى لام تليها ألف مجردة، ويبقى البحث بأيٍّ من الحرفين يصل إلى الاسم. |
| `ar.tatweel` | `canonical` | `arab` | التطويل يمدّ الحرف لأجل الإخراج الطباعي ولا يقول شيئًا عن الاسم، فالكتابة الممدودة يجب أن تصل إلى المفتاح نفسه الذي تصل إليه الكتابة غير الممدودة. |
| `ar.marks` | `canonical` | `arab` | الحركات والشدّة والسكون والألف الخنجرية والهمزة العالية وعلامات الضبط القرآني ضبطٌ قد يكتبه المصدر وقد لا يكتبه، ولا أحد يكتبها في خانة البحث. |
| `latn.combining-marks` | `canonical` | `latn` | العلامة اللاتينية المفكّكة هي الكتابة نفسها للعلامة المركّبة، فحذف العلامات المركّبة يجمع بين الصورتين دون أن نطلب من بيئة التشغيل شيئًا. |
| `any.invisible` | `canonical` | `any` | الشرطة اللينة والمحارف عديمة العرض وعلامات اتجاه الكتابة وعلامة ترتيب البايتات لا تُرى على الشاشة، فتُحذف بدل أن تقطع الاسم أو تغيّره. |
| `ar.alef-variants` | `conservative` | `arab` | الألف بهمزة فوقها أو تحتها، وبالمدّة، وألف الوصل، تُكتب كلها للحرف نفسه، والناس يكتبونها ألفًا مجردة في العادة. |
| `ar.waw-hamza` | `conservative` | `arab` | الهمزة المكتوبة على الواو عادة إملائية لمصدر بعينه، وليست حرفًا آخر يُبحث عنه. |
| `ar.yaa-hamza` | `conservative` | `arab` | الهمزة المكتوبة على الياء عادة إملائية لمصدر بعينه، وليست حرفًا آخر يُبحث عنه. |
| `ar.indic-digits` | `conservative` | `arab` | نوع الأرقام التي تخرجها لوحة المفاتيح يجب ألا يغيّر أي الأماكن موجود، فالأرقام العربية الهندية هي الأرقام اللاتينية التي تعدّها. |
| `ar.extended-indic-digits` | `conservative` | `arab` | الأرقام العربية الهندية الشرقية هي الأعداد نفسها بصور أخرى، والمصدر الذي يستعملها يسمّي المكان نفسه. |
| `latn.accents` | `conservative` | `latn` | الاسم الفرنسي يُكتب بلا علاماته أكثر بكثير مما يُكتب بها، فحروف كتلة Latin-1 المشكّلة تعود إلى حرفها الأصلي. |
| `latn.extended-a` | `conservative` | `latn` | كتلة Latin Extended-A تحمل الفكرة نفسها في مدى أبعد: علامات الكتابات المنقولة حرفيًا ورابطة oe الفرنسية، وكلها تعود إلى الحروف المكتوبة تحتها. |
| `latn.extended-b` | `conservative` | `latn` | الحروف المشكّلة في كتلة Latin Extended-B، ومنها علامة الـ g في الكتابة البربرية باللاتينية، تعود إلى حرفها الأصلي؛ أما حروف تلك الكتلة التي هي حروف قائمة بذاتها فتبقى على حالها ولا تفقد إلا كِبَرها. |
| `any.separators` | `conservative` | `any` | الاسم طلبُ بحثٍ واحد سواء كُتب بفاصلة عليا أو شَرطة أو مطّة أو مسافة، فكلٌّ من هذه ينهي كلمة بدل أن يلصق المفتاح أو يقطعه على نحو مختلف. |
| `any.punctuation` | `conservative` | `any` | الفاصلة والنقطة والقوس وعلامة التنصيص تحيط بالاسم ولا تكون منه، في الكتابتين معًا، فتنهي كلمة بدل أن تدخل في المفتاح: المفتاح لا يحمل ترقيمًا أبدًا، ومحرّك الفهرسة النصية الذي يبني الفهرس يقطع في المواضع نفسها التي تقطع فيها هذه الحزمة. |
| `any.whitespace` | `conservative` | `any` | الفواصل المكرّرة وفي أول الاسم من الكتابة لا من التسمية: الفاصل الذي لا يجد كلمة مفتوحة لا يضيف شيئًا إلى المفتاح، فالمفتاح هو الكلمات موصولة بمسافة واحدة، وتبقى الكلمة الأخيرة الناقصة قابلة للإكمال. |
| `any.case` | `conservative` | `any` | حالة الحرف لا تفرّق بين مكانين أبدًا، والحالة الصغيرة هي ما ينتجه فهرس المتصفح ومحرّك الفهرسة النصية أصلًا. ويطال ردُّ الحالة الحروفَ اللاتينية الكبيرة وكلَّ حرف كبير يسمّيه جدول؛ أما تصغير حرف لا يسمّيه أي جدول فيعني سؤال المحرّك عن نظيره الصغير، وهو الاعتماد الذي ترفضه هذه الحزمة. |
| `any.pass-through` | `conservative` | `any` | الحرف الذي لا يسمّيه أي جدول يبقى كما هو ولا يُحذف، لأن بقاء الاسم قابلًا للبحث بحرف لا رأي لهذه الحزمة فيه خير من اختصاره في صمت. |
| `ar.alef-maqsura-yaa` | `loose` | `arab` | الألف المقصورة والياء تُكتب إحداهما مكان الأخرى في آخر الاسم، وكثيرًا ما يفعل ذلك المصدر نفسه، لكنهما تبقيان حرفين مختلفين، فالمساواة بينهما تنتمي إلى الطبقة التي يمكن أن تُرتَّب المطابقة الناتجة عنها في مرتبة أدنى. |
| `ar.taa-marbuta-haa` | `loose` | `arab` | الاسم المنتهي بتاء مربوطة كثيرًا ما يُكتب بهاء، والعكس كذلك، لكن القارئ يرى حرفين، فالمساواة بينهما تنتمي إلى الطبقة التي يمكن أن تُرتَّب المطابقة الناتجة عنها في مرتبة أدنى. |
| `ar.definite-article` | `declined` | `arab` | أداة التعريف العربية لا تُحذف في أي من المفتاحين. وكون الاسم بالأداة والاسم بدونها يدلّان على المكان نفسه حقيقةٌ عن ذلك المكان لا عن الكتابة، فمكانها اسمٌ بديل لذلك المكان له مصدره. |
| `latn.transliteration` | `declined` | `any` | لا يُولَّد أي نقل حرفي لاتيني لاسم عربي، ولا أي صورة عربية لاسم لاتيني. الكتابة التي لا يوفّرها أي مصدر اسمٌ مختلق، والمنتجات تنشر الأسماء ولا تخترعها. |

الترتيب أعلاه هو ترتيب تطبيق مسار المفتاح للقواعد، والقاعدتان المرفوضتان في آخره. وفي هذا
المستودع يفشل `pnpm validate` إذا كانت قاعدة بلا سجلّ مراجعة، أو لم تثبتها حالة من المدوّنة، أو
سمّت حالةٌ قاعدةً ليست في الجدول، أو تكرّر معرّف، أو خالف ترتيبُ الجدول الترتيبَ المراجَع المحفوظ
بجانب الفحص.

إن كنت تقرأ العربية أو الفرنسية ورأيت جملة من هذه الجمل خطأً، فهذا بالضبط طلبُ الدمج الذي
نُشرت هذه الحزمة لتستقبله: غيّر القاعدة، أو حالة المدوّنة التي تثبتها، وقل لماذا.

## أي القواعد عملت

تُرجع `explain` كلَّ ما تُرجعه `searchKeys`، ومعه `applied`: معرّفات القواعد التي عملت على ذلك
المدخل، بترتيب تطبيقها. وبذلك تستطيع المطابقة الأوسع أن تقول دائمًا ما الذي جعلها أوسع، وهو ما
يتيح للترتيب أن يضعها دون المطابقة التامة:

```js
import { explain } from "@geoalgeria/normalize";

explain("قسنطينة").applied;   // ["ar.taa-marbuta-haa"]
explain("الجـــزائر").applied;  // ["ar.tatweel", "ar.yaa-hamza"]
explain("Béjaïa").applied;      // ["any.case", "latn.accents"]
explain("الوادي").applied;     // []، لم تمسّ هذا الاسم أي قاعدة
```

تدخل القاعدة في القائمة إذا غيّرت نقطة برمجية واحدة على الأقل أو أنهت كلمة واحدة على الأقل. أما
القواعد التي تنصّ على طيّ لا تطبّقه هذه الحزمة، وهي قاعدة الإبقاء والقاعدتان المرفوضتان، فلا تدخل
القائمة أبدًا: تثبتها المدوّنة في الاتجاه المعاكس، بحالةٍ تُظهر مفاتيحُها المتوقَّعة بقاءَ الحرف أو
بقاءَ الأداة. و`explain` هي مسار المفتاح نفسه وقد شُغّل التسجيل، لا تنفيذ ثانٍ له، فلا يمكن أن
تخالف `searchKeys`.

## الاستقلال عن بيئة التشغيل

مسار المفتاح يملك جداول نقاط الترميز الخاصة به. لا يستدعي شيئًا من آليات يونيكود في المحرّك،
ولا يستعمل أصناف الخصائص، ولا أي عملية تغيير حالة تابعة للغة المحلية، ولا يستورد أي وحدة أصلية
من المنصة، فيُنتج البايتات نفسها على Node وعلى الهاتف، ولا يمكن لترقية المحرّك أن تغيّر مفتاحًا
منشورًا في صمت. اختبارٌ يفرض ذلك بقراءة الشيفرة نفسها لا بالثقة في تعليق.

وتُعلن الجداول: ASCII، والملحق اللاتيني-1، واللاتينية الموسّعة أ وب، وعلامات التشكيل المركّبة،
والكتلة العربية، والملحق العربي، والعربية الموسّعة أ، وكتلتَي أشكال العرض العربية. داخل هذه
الكتل تُحذف العلامات وتُطوى الصور؛ أما الحروف التي هي حروف قائمة بذاتها، كغين البربرية
اللاتينية، والقاف المعقودة المغاربية في الملحق العربي، والباء الفارسية والفاء الثلاثية والكاف
المعقودة، فتبقى كما هي. والحرف الخارج عن كل كتلة معلنة يُردّ إلى الصغيرة إن كان حرفًا لاتينيًا
كبيرًا، وإلا مُرّر كما هو ولم يُحذف أبدًا.

الحزمة **بلا أي اعتماديات تشغيل**.

## المدوّنة المرجعية

المدوّنة هي العقد، وفيها 64 حالة. كل قاعدة أعلاه تثبتها حالةٌ واحدة على الأقل مبنية على اسم جزائري حقيقي،
ويستورد المستهلكون المدوّنة نفسها بدل كتابة حالاتهم الخاصة:

</div>

```js
import { corpus } from "@geoalgeria/normalize/fixtures";

for (const kase of corpus) {
  // kase.input, kase.conservative, kase.loose, kase.tokens, kase.proves, kase.note
}
```

<div dir="rtl">

## الإصدارات

`NORMALIZE_VERSION` هو الرقم الأكبر في نسخة الحزمة. أي تغيير فيما ترجعه دالة مفتاح لأي مدخل هو
إصدار **أكبر**، حتى لو كان تصحيح خلل، لأن المفاتيح مخبوزة في الفهارس المنشورة والفهرس المثبّت لا
يُرحَّل سجلًا سجلًا. إضافة تصدير جديد إصدار أصغر؛ التوثيق والأنواع ترقيع. ويثبّت المستهلكون نسخة
محددة بالضبط.

## الرخصة

MIT. هذه الحزمة تشحن شيفرة فقط، دون أي بيانات.

</div>
